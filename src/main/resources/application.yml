spring:
  application:
    name: Sanguosha1v1
  main:
    allow-circular-references: true

logging:
  level:
    root: info
  pattern:
    console: "%msg%n"

app:
  one-config:
    name: "孙坚"
    hp: 4
    maxHp: 10
    hand-cards: [ ]
  two-config:
    name: "10000血白板"
    hp: 10000
    maxHp: 10000
    hand-cards: [ ]
